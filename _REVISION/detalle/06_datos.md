# 06 · Datos — Revisión profunda 2026-08-27/28

> Generado por el subagente A6·DATOS. Rama `revision/2026-08-27`. Construido sobre `_REVISION/detalle/01_inventario.md` (A1) y `_REVISION/detalle/02_grafo.md` (A2) — no se repite el hash-scan ni el mapeo de llamadas ya hechos por ellos; este documento profundiza en parseo/contenido/consistencia de formato de los archivos de datos individuales (fuera de las 5 carpetas-dataset agregadas de A1) y, en particular, en los archivos nuevos de la sesión de hoy (27/28-ago-2026, ver `git status` al inicio). Ningún script del proyecto fue ejecutado (regla 9); ningún `.mat`/`.csv` completo se cargó en memoria — lectura de cabecera + muestreo con `head`/`wc -l`/`grep`/`readlines` parcial y `zipfile.testzip()` para el `.zip`.

---

## 1. Tabla de hallazgos

| ruta | tipo | comprobación | productor | consumidor | observación | candidato Ronda 3 |
|---|---|---|---|---|---|---|
| `CODIGOS/GENERADOR/TOBILLO/DIAG_ladotrick_Tobillo_resultados.csv` | .csv (nuevo, 27-ago) | **OK** — parsea, cabecera `sub_id,config,r_x,rmse_x,r_y,rmse_y` coincide exactamente con `writetable(...,'VariableNames',{'sub_id','config','r_x','rmse_x','r_y','rmse_y'})` de la línea 128 del script; 61 filas = 1 header + 15 sujetos × 4 configs (`Zhao_izquierda_nativo`, `Zhao_derecha_alt`, `Yun_R_nativo`, `Yun_L_alt`), sin huecos, sin `NaN` | `CODIGOS/GENERADOR/TOBILLO/DIAG_ladotrick_Tobillo.m:132` (`writetable(R, fullfile(carpeta,'DIAG_ladotrick_Tobillo_resultados.csv'))`) | **ninguno encontrado** — ningún `.m` lo lee, ningún `.md` (ni `CIERRE_TOBILLO.md`, ni `JUSTIFICACION_MODELOS_Y_ESTADO_Q1.md`, que sí menciona el script `.m` en su línea 313 pero no esta salida) narra sus números todavía | Resultado de un experimento "diagnóstico" (`DIAG_*`, no `Evaluar_*`) explícitamente declarado en el propio script como versión simplificada para responder rápido una pregunta abierta (`CIERRE_TOBILLO.md #11`); es coherente que sea de hoy y aún no esté interpretado en prosa | No (es dato nuevo válido, en espera de interpretación — no ruido) |
| `CODIGOS/GENERADOR/INCLINACION_TIBIAL/DIAG_ladotrick_AnguloTibial_resultados.csv` | .csv (nuevo, 27-ago) | **OK** — cabecera `sub_id,config,r_crudo,r_cal,rmse_cal,rmsenorm_cal,ganancia` coincide con `VariableNames` de la línea 87 del script; 61 filas = 1 header + 15×4, sin `NaN` real (1 falso positivo de `grep -i nan`: la palabra "ganancia" contiene la subcadena "nan") | `CODIGOS/GENERADOR/INCLINACION_TIBIAL/DIAG_ladotrick_AnguloTibial.m:92` | **ninguno encontrado**, mismo caso que el de TOBILLO | Hallazgo numérico visible en los datos: para los 4 candidatos "alternativos" (Zhao/Yun) `ganancia` sale **negativa** (~-0.21) y `r_cal = abs(r_crudo)` exactamente — esto **no es un error de parseo ni un bug**, está documentado y explicado como comportamiento matemático esperado en `CODIGOS/GENERADOR/REPORTE_NOCHE.md:44` (la calibración afín solo puede invertir el signo del error cuando el candidato tiene la forma opuesta a la real, no corregirla) | No (dato consistente con el mecanismo ya documentado, no un archivo corrupto) |
| `CODIGOS/GENERADOR/TOBILLO/yun_run_log.txt` | .txt (nuevo, 27-ago) | **OK** — texto UTF-8/ASCII válido, abre sin error; contenido es una captura de consola/diary de MATLAB (progreso de regresión GP + tablas de resultados impresas) | **No es un archivo escrito por ningún script del proyecto** (`grep` de `diary`/`fopen.*yun_run_log` en todo `CODIGOS/GENERADOR/` no encontró nada) — es una captura manual de sesión de terminal al correr `Evaluar_vs_Kuopio_Tobillo_Fases_Yun.m` y `Evaluar_Individual_Kuopio_Tobillo_Yun.m` en secuencia (confirmado por el contenido: termina imprimiendo exactamente las tablas `ans = ... table` que esos 2 scripts producen por `disp`/salida de consola) | **ninguno** | La propia documentación del proyecto (`CODIGOS/GENERADOR/REPORTE_NOCHE.md:53`, escrita la noche del 26/27-ago) ya identifica este archivo como "log de depuración, no resultado" y dice textualmente: *"quedaron sin commitear a propósito... bórralos si ya no los necesitas"*. `BITACORA_NOCHE.md:55-79` confirma el origen (verificación manual de que un proceso MATLAB en background seguía vivo). Los resultados finales que este log solo *reproduce en texto* ya existen como CSV+PNG **committeados** (`Evaluar_vs_Kuopio_Tobillo_Fases_Yun_resultados.csv`, `Evaluar_Individual_Kuopio_Tobillo_Yun_resultados.csv`, ambos ya en el inventario de A1 como archivos trackeados) | **Sí — candidato fuerte a eliminar**: ruido de depuración transitorio, ya superado por resultados finales committeados, y el propio proyecto ya recomendó borrarlo |
| `CODIGOS/GENERADOR/INCLINACION_TIBIAL/yun_grupo_log.txt` | .txt (nuevo, 27-ago) | **OK** — mismo tipo de contenido (captura de consola de `Evaluar_vs_Kuopio_AnguloTibial_Yun.m`, corrida de grupo) | Igual que arriba: captura manual de terminal, no escrito por código | **ninguno** | Mismo caso — `REPORTE_NOCHE.md:53` lo nombra explícitamente junto a los otros 2 | **Sí — candidato fuerte a eliminar**, mismo motivo |
| `CODIGOS/GENERADOR/INCLINACION_TIBIAL/yun_individual_log.txt` | .txt (nuevo, 27-ago) | **OK** — captura de consola de `Evaluar_Individual_Kuopio_AnguloTibial_Yun.m` | Igual, captura manual | **ninguno** | Mismo caso | **Sí — candidato fuerte a eliminar**, mismo motivo |
| `CODIGOS/GENERADOR/RODILLA/Fukuchi/Evaluar_vs_Fukuchi2018_KoopmanZhao_resultados.csv` | .csv (nuevo, carpeta `RODILLA/Fukuchi/` completa sin trackear) | **OK** — cabecera `sub_id,articulacion,candidato,r_crudo,r_cal,rmse_cal` coincide con `VariableNames` (línea 97 del script); 211 filas = 1 header + 210; **5 combinaciones articulación×candidato de 6 posibles** (`cadera,Koopman`/`cadera,Zhao`/`rodilla,Koopman`/`rodilla,Zhao`/`tobillo,Koopman`, 42 filas c/u — falta `tobillo,Zhao`) | `CODIGOS/GENERADOR/RODILLA/Fukuchi/Evaluar_vs_Fukuchi2018_KoopmanZhao.m:103` | La figura derivada (`Evaluar_vs_Fukuchi2018_KoopmanZhao_figura.png`) sí se embebe en `docs/algoritmo/JUSTIFICACION_MODELOS_Y_ESTADO_Q1.md:293` (confirmado por A2) | La combinación faltante `tobillo,Zhao` **no es un error de generación**: el propio script lo declara en comentario (línea 38, `Pred_tobillo = struct('Koopman', nan(n,101))  % Zhao no predice tobillo`) — Zhao 2026 no modela el segmento de tobillo, es una limitación de diseño del candidato, no un fallo de esta corrida. Yun también está ausente de toda la tabla, declarado explícitamente en el encabezado del script (líneas 2-17: excluido por lentitud de su regresión GP bajo presión de memoria de la máquina esa noche, "pendiente por separado") | No (asimetría de columnas es por diseño, documentada en el propio código; los 42/42 sujetos que sí corrieron no tienen fallas ni NaN) |
| `docs/literatura/pdfs/22151474.zip` | .zip (nuevo, 27-ago) | **OK** — `zipfile.testzip()` sin error, 262 entradas internas, incluye `Readme.docx` + 6 `Base_de_datos_*.csv` + decenas de `Prom_cinematica*.csv` | Descarga externa (dataset de marcha, aparentemente chileno, sin script propio que lo genere) | `docs/algoritmo/busqueda_modelos_antropometria_rodilla.md:30` (mención documental) — **sin ningún `.m` consumidor todavía** (confirmado también por A2 §3 ítem 5) | Contiene íntegros, entre sus 262 archivos, los 2 CSV que además existen extraídos por separado en `chile_extract/` (ver fila siguiente) — confirmado por hash SHA-256 byte-idéntico | No (archivo de literatura/dataset candidato, en evaluación activa según su propio doc de referencia — no es ruido) |
| `docs/literatura/pdfs/chile_extract/Base_de_datos_cinematica.csv` | .csv (nuevo, 27-ago) | **OK** — parsea, 24 columnas (`PXg_m...AZg_m` con sufijos `_m`/`_sd`, media/SD por variable), 101 filas de datos (0-100% de ciclo de marcha, consistente con curva normalizada) | Extraído manualmente de `22151474.zip` (**confirmado por hash SHA-256 idéntico byte a byte** entre el archivo dentro del zip y el archivo suelto: `bb21a7df...`, 20160 bytes en ambos) | Ninguno (misma mención documental que el zip, sin consumidor de código) | **Duplicado de contenido exacto** respecto a la copia dentro de `22151474.zip` — es una extracción de conveniencia (2 de 262 archivos) para inspección rápida sin descomprimir el zip completo, no una regeneración independiente | No (redundancia trivial, ~24KB total, documentada por su propio propósito de "extracto para revisar" — no amerita limpieza en Ronda 3, pero si se decide quedarse solo con una copia, la candidata a retener es el `.zip` completo por ser la fuente y el `chile_extract/` el prescindible) |
| `docs/literatura/pdfs/chile_extract/Datos_generales_y_VTE.csv` | .csv (nuevo, 27-ago) | **OK** — parsea, 16 columnas (antropometría + parámetros temporo-espaciales por sujeto: `sexo,edad,masa,estatura,imc,cadencia,velocidad,...`), 51 filas de sujetos | Igual que arriba — extraído de `22151474.zip`, hash idéntico confirmado (`df7c62ef...`, 3852 bytes) | Ninguno | Mismo caso de duplicado intencional de conveniencia | No, mismo motivo que la fila anterior |
| `docs/literatura/pdfs/DeLeva1996_JBiomech_SegmentInertiaParameters.pdf` | .pdf (nuevo, 27-ago) | **OK** — abre, cabecera `%PDF-1.3` válida, 1.4MB | Descarga externa (literatura) | Probable respaldo bibliográfico de `CODIGOS/GENERADOR/MasaSegmentaria_DeLeva1996_Core.m` (coincidencia temática fuerte: el script cita explícitamente "de Leva 1996 (Table 4)" en su encabezado, líneas 3-30) **pero sin cita textual verificable** — el nombre del PDF no aparece referenciado por ruta en ningún `.m`/`.tex`/`.bib` del repo (grep dirigido, 0 coincidencias fuera de `_REVISION/`) | Vínculo PDF↔script es por coincidencia de nombre/tema, no por referencia explícita en código — la verificación de que los coeficientes de `MasaSegmentaria_DeLeva1996_Core.m` coinciden con la Tabla 4 real del PDF es una tarea de verificación bibliográfica/numérica, fuera del alcance de este subagente (parseo de formato, no de contenido científico) — señalado para el subagente de bibliografía/citas si existe en esta ronda | No (PDF válido, en uso aparente aunque sin cita formal todavía) |
| `docs/literatura/pdfs/Sudeesh 2024.pdf` | .pdf (ya reportado por A1/A2, no nuevo hallazgo de A6) | **OK** — abre, cabecera `%PDF-1.7` válida, 12.2MB | Descarga externa | Citado en el manuscrito (`Sudeesh2024CompactGaitSimulator`, confirmado por A2 §1.4) | Sin hallazgo adicional de A6 más allá de lo ya documentado por A1 (bloqueo 403 aparentemente superado) y A2 (cita↔bib↔PDF resuelto) | No |
| `CODIGOS/GENERADOR/GRF_Newton_ApoyoSimple_Core.m` | .m (nuevo, 28-ago) — **no es archivo de datos**, incluido solo porque su rol es relevante para el resto de esta tabla | N/A (fuera de alcance de A6, es código) | — | — | Confirmado (grep dirigido): **no escribe ningún archivo de datos** (`writetable`/`save`/`csvwrite`/`xlswrite` — 0 coincidencias), solo retorna un `struct` en memoria. Ya reportado por A2 como huérfano de llamadas (0 llamadores, 0 menciones documentales) — consistente con esta revisión: tampoco produce ningún artefacto de datos que rastrear todavía | Fuera de alcance de A6 (candidato de código, no de datos — ver `02_grafo.md` §3 ítem 1) |
| `CODIGOS/GENERADOR/MasaSegmentaria_DeLeva1996_Core.m` | .m (nuevo, 27/28-ago) — no es archivo de datos | N/A | — | `GRF_Newton_ApoyoSimple_Core.m:120` (único llamador, confirmado por A2) | Igual que arriba: confirmado (grep) que no escribe ningún archivo de datos, solo retorna fracciones de masa/CoM en un `struct` — sus "datos" son constantes numéricas embebidas en el propio código (tabla de De Leva 1996), no un archivo externo que parsear | Fuera de alcance de A6 |

---

## 2. Duplicados por hash más allá de lo ya encontrado por A1 (ítem 4 del encargo)

Búsqueda dirigida en los archivos de **resultados** nuevos de hoy (no en datasets agregados, ya cubiertos por A1): **no se encontraron pares de CSV/TXT de resultados con contenido idéntico entre sí** (los 3 logs `yun_*` tienen tamaños distintos entre sí — 13229/13759/24477 bytes — y contenidos distintos, cada uno documenta una corrida distinta; los 2 CSV `DIAG_ladotrick_*` tienen columnas distintas entre sí). El único duplicado real encontrado es el ya descrito en la tabla: `chile_extract/*.csv` es un subconjunto extraído **byte-idéntico** de `22151474.zip` — confirmado con SHA-256 completo (no solo tamaño), no es una corrida repetida sino una extracción de conveniencia declarada en `docs/algoritmo/busqueda_modelos_antropometria_rodilla.md`.

## 3. Formato esperado vs. real (ítem 3 del encargo)

Los 3 scripts `.m` que documentan explícitamente en código qué columnas escriben (`DIAG_ladotrick_Tobillo.m`, `DIAG_ladotrick_AnguloTibial.m`, `Evaluar_vs_Fukuchi2018_KoopmanZhao.m`) coinciden **exactamente** con las cabeceras reales de sus CSV de salida — 0 discrepancias de nombre, orden o tipo de columna encontradas en los 3 casos verificados.

## 4. Corruptos o vacíos (ítem 6 del encargo)

Ninguno. Los 2 `.m` nuevos revisados (`GRF_Newton_ApoyoSimple_Core.m`, 15012 bytes; `MasaSegmentaria_DeLeva1996_Core.m`, 6195 bytes) no están vacíos. Ningún CSV/TXT/ZIP/PDF de los revisados en esta ronda tiene 0 bytes ni falla al parsear/abrir.

---

## Resumen

- **Archivos de datos revisados a fondo en esta ronda (fuera de lo ya cubierto por A1/A2):** 12 — 2 CSV `DIAG_ladotrick_*`, 3 logs `yun_*`, 1 CSV de Fukuchi, 1 `.zip` (262 entradas internas verificadas con `testzip`), 2 CSV de `chile_extract/`, 2 PDF nuevos, más verificación cruzada de 2 `.m` nuevos que resultaron no producir datos.
- **Nº FALLA (parseo roto o corrupción):** 0.
- **Nº sin consumidor (huérfanos de datos):** 6 de los 12 — los 2 CSV `DIAG_ladotrick_*` (esperado, son de hoy, aún sin interpretar en prosa), los 3 logs `yun_*` (huérfanos permanentes por diseño — nunca tuvieron consumidor de código, solo eran evidencia de progreso), y el `.zip`/`chile_extract` (dataset candidato en evaluación, sin cargador `.m` todavía).
- **Hallazgo más importante:** los 3 logs `yun_run_log.txt`/`yun_grupo_log.txt`/`yun_individual_log.txt` son ruido de depuración transitorio que la propia documentación del proyecto (`CODIGOS/GENERADOR/REPORTE_NOCHE.md:53`, escrita por el propio equipo la noche del 26/27-ago) ya identificó y pidió borrar explícitamente — los resultados que reproducen en texto plano ya existen, completos y committeados, como CSV+PNG. Es el candidato de limpieza más claro y con mejor evidencia de todo este informe.
- **Segundo hallazgo:** los 2 CSV `DIAG_ladotrick_*` de hoy están bien formados y completos (61 filas cada uno, sin `NaN` real, cabeceras exactas), pero muestran una anomalía numérica ya explicada por el propio equipo (`ganancia` negativa, `r_cal=|r_crudo|` para los candidatos Zhao/Yun) — dato válido, no defecto de archivo.
- **Tercer hallazgo:** `chile_extract/*.csv` es una copia de conveniencia, byte-idéntica, de 2 de los 262 archivos dentro de `22151474.zip` — redundancia trivial (24KB) y documentada, no amerita limpieza, pero es información útil para Ronda 3 si se decide simplificar `docs/literatura/pdfs/`.
