# 03 · Código — Revisión profunda 2026-08-27/28

> Generado por el subagente A3·CÓDIGO. Rama `revision/2026-08-27`. Construido sobre `01_inventario.md` (A1, `mlint` ya corrido: 0 errores de sintaxis en 92 `.m`, 35 con avisos de estilo/rendimiento — no repetido aquí) y `02_grafo.md` (A2, grafo de llamadas y grado de entrada/salida — reusado, no repetido). Método: lectura directa de los archivos núcleo de `CODIGOS/GENERADOR/` (los que `CLAUDE.md` describe con más detalle) contra su propia documentación (`GUIA_INTERPRETACION.md`, `docs/planificacion/plan_ensamble_multimodelo.md`) y contra el resto del código que los llama; grep dirigido de `TODO/FIXME/XXX/PENDIENTE`, rutas absolutas, y `try/catch`. Ningún script fue ejecutado (regla 9). Todo hallazgo cita `ruta:línea` verificada por lectura directa en esta sesión.

**Alcance cubierto en profundidad (lectura completa, línea a línea):** los 10 archivos que `CLAUDE.md` señala como núcleo del generador (`Generar_Trayectoria.m`, `Escribir_CSV_Simulador.m`, `Koopman2014_Core.m`, `Zhao2026_Core.m`, `Yun2014_Wrapper.m`, `Romero_Sorozabal2024_Core.m`, `Combinar_Candidatos_Core.m`, `Cadena_Cinematica_Core.m`, `Cadena_Completa_Core.m`, `Estimar_Antropometria_Core.m`, `Estimar_Velocidad_Froude_Core.m`) más `Reduccion_Winter_Core.m`, `Obtener_Theta_Tibia_Candidato.m`, `Obtener_Angulos_Candidato.m`, `Segmento_Posicion_Core.m`, `Temporizacion_Core.m`, `Tiempo_Ciclo_Koopman2014_Core.m`, `GRF_Newton_ApoyoSimple_Core.m`, `MasaSegmentaria_DeLeva1996_Core.m`, `Cargar_Camargo_Core.m`, `SPM1D_Core.m`, `Procesar_Multisujeto_Core.m`, `Cargar_Sujetos_CSV.m`, `Calcular_Metricas_Curva.m` (primeras 40 líneas) — 24 archivos. **Cubierto superficialmente** (grep dirigido de patrones de riesgo, sin lectura línea a línea): los ~68 archivos restantes de `CODIGOS/` (los `Evaluar_*`/`DIAG_*` de `RODILLA/`, `TOBILLO/`, `INCLINACION_TIBIAL/`, `Fukuchi/`, `Ferber/`, `Kuopio/`, y el resto de `CALIBRACION/`, `ESTADISTICA/`, `POTENCIA_EQUIVALENCIA/`, `INCERTIDUMBRE/`, `VALIDACIONES/`, `GENERAR CURVS DE REFERENCIA/`).

---

## CODIGOS/GENERADOR/ (núcleo del pivote — prioridad alta)

| archivo:línea | tipo | qué pasa | evidencia | severidad | corrección propuesta (diff, sin aplicar) | confianza |
|---|---|---|---|---|---|---|
| `Cadena_Cinematica_Core.m:100-111` y `GUIA_INTERPRETACION.md:121` (§3-bis) | doc vs. código desactualizada | Ambos documentan la regla E2 **anterior** al 24-ago-2026: *"Koopman: via_tobillo en apoyo, via_rodilla en balanceo"* / *"vía-tobillo como principal durante apoyo... vía-rodilla se calcula y se reporta como diagnóstico cruzado, pero no alimenta directamente la posición de la rodilla"*. La regla **actual y vigente** (verificada en el código que realmente se ejecuta) es: Koopman usa **vía-rodilla en AMBAS fases** desde el 24-ago-2026, con evidencia cuantitativa propia (r=0.982 vs. r=-0.435 contra `Control_Luis` real) que motivó el cambio. | `Cadena_Cinematica_Core.m:103-105` (código fuente, sección "REGLA POR CANDIDATO") vs. `Obtener_Theta_Tibia_Candidato.m:43-89` (implementación real, con el comentario explícito "CORREGIDO 24-ago-2026... Se usa via_rodilla en AMBAS fases"); mismo texto obsoleto repetido en `CODIGOS/GENERADOR/GUIA_INTERPRETACION.md:121` | alta | En `Cadena_Cinematica_Core.m:103-105`, reemplazar el bloque "REGLA POR CANDIDATO" para que describa la regla vigente (vía-rodilla ambas fases para Koopman, vía-tobillo ambas fases para Yun, nativo para Zhao) o, mejor, borrar la duplicación de la regla en este archivo y remitir a `Obtener_Theta_Tibia_Candidato.m` como única fuente de verdad (ya lo es en la práctica desde que se extrajo el 24-ago-2026). En `GUIA_INTERPRETACION.md:121`, agregar una nota de "SUPERADO 24-ago-2026" apuntando a §7 (donde sí está el cambio real) o reescribir la frase. | alta (leídos ambos archivos directamente en esta sesión) |
| `Combinar_Candidatos_Core.m:108-124` | riesgo de signo no verificado, ignorado silenciosamente | `Romero_Sorozabal2024_Core.m:39-47` declara EXPLÍCITAMENTE: *"NO combinar con los otros 3 candidatos sin revisar el signo de X primero"* (el paper no declara el signo de avance en el texto, solo en una figura). `Combinar_Candidatos_Core.m` SÍ combina ese eje X directo (`x_rodilla_rel_tobillo_m = RS.rodilla.x_m - RS.tobillo.x_m`, línea 112) en el promedio final (líneas 121 y 123) sin ningún chequeo, corrección o comentario que resuelva la advertencia — a diferencia del resto del pipeline, donde cada corrección de signo está verificada contra dato real y documentada (p.ej. la "NOTA DE SIGNO (G7 CERRADO)" de `Cadena_Cinematica_Core.m:81-93`). El propio plan del ensamble reconoce el síntoma sin diagnosticarlo: *"los 4 candidatos discrepan bastante entre sí en X (rango hasta ~60cm)"*, sin comprobar si la causa es este signo no verificado. | `Romero_Sorozabal2024_Core.m:39-47`; `Combinar_Candidatos_Core.m:108-124`; `docs/planificacion/plan_ensamble_multimodelo.md:141` | alta | Antes de promediar, verificar el signo de X de Romero-Sorozábal contra una referencia real (p.ej. `Cargar_Camargo_Core.m` o `Control_apoyo_Luis_V4.csv`, mismo método ya usado para K/Z/Y en `Cadena_Cinematica_Core.m:81-93`) y aplicar el signo correcto explícitamente en `Combinar_Candidatos_Core.m:112`, con el mismo patrón de comentario "NOTA DE SIGNO... verificado con datos reales" que usa el resto del archivo. Hasta entonces, declarar el resultado de `candidato='Combinado'` como no válido para reportar/comparar en el manuscrito. | alta (leídos los 2 archivos y el plan directamente) |
| `Generar_Trayectoria.m:139,153` y `GRF_Newton_ApoyoSimple_Core.m:125,133` | ineficiencia / cómputo redundante | `Obtener_Theta_Tibia_Candidato` (línea 139) y `Obtener_Angulos_Candidato` (línea 153) reciben los MISMOS `(candidato, antro, tempo, n)` pero cada una vuelve a correr el modelo completo del candidato desde cero (Koopman/Zhao/Yun) en vez de que la segunda reuse el resultado de la primera — determinístico, así que no cambia el resultado, pero duplica el costo. Para `candidato='Yun'` esto es particularmente caro: `Yun2014_Wrapper.m:13-16` documenta que el toolbox de terceros (`Gait_Pred.m`, GPR) **"guarda 15 archivos .mat y 15 .png dentro de su propia carpeta y abre figuras"** como efecto colateral — cada llamada a `Generar_Trayectoria(..., 'Yun')` corre esto DOS veces, escribiendo/sobrescribiendo 30 archivos y abriendo/cerrando figuras dos veces. Mismo patrón exacto en `GRF_Newton_ApoyoSimple_Core.m:125` (`Obtener_Theta_Tibia_Candidato`) y `:133` (`Obtener_Angulos_Candidato`). | `Generar_Trayectoria.m:139,153`; `GRF_Newton_ApoyoSimple_Core.m:125,133`; `Yun2014_Wrapper.m:13-16` | media | Hacer que `Obtener_Theta_Tibia_Candidato.m` devuelva también `theta_muslo_apoyo/balanceo` (ya calcula `theta_muslo_rad` internamente para el camino vía-rodilla de Koopman/Yun, líneas 87/102/143 de `Yun2014_Wrapper.m` y `Koopman2014_Core.m`) para que `Generar_Trayectoria.m`/`GRF_Newton_ApoyoSimple_Core.m` no necesiten una segunda llamada a `Obtener_Angulos_Candidato`. | alta (código leído; el efecto colateral de I/O está documentado por el propio autor del wrapper) |
| `CODIGOS/VALIDACIONES/Calcular_Metricas_Curva.m:1` llamado desde `CODIGOS/MULTISUJETO/Procesar_Multisujeto_Core.m:128` | inconsistencia de convención / riesgo de portabilidad | El archivo se llama `Calcular_Metricas_Curva.m` (PascalCase, como TODOS los demás `*_Core.m`/con nombre propio del proyecto — p.ej. `Koopman2014_Core.m` define `function out = Koopman2014_Core(...)`, exactamente el mismo case que el nombre de archivo), pero la función que contiene se llama `calcular_metricas_curva` (minúsculas). Se invoca en minúsculas desde `Procesar_Multisujeto_Core.m:128`. En Windows (NTFS, insensible a mayúsculas) esto resuelve sin error; en un sistema de archivos sensible a mayúsculas (Linux; y `CLAUDE.md` documenta que el 22-ago-2026 se sincronizó una laptop Mac con el mismo repo) el comportamiento de resolución de nombre de función de MATLAB por archivo+nombre puede fallar o dar un warning de "wrong case". | `CODIGOS/VALIDACIONES/Calcular_Metricas_Curva.m:1`; `CODIGOS/MULTISUJETO/Procesar_Multisujeto_Core.m:128` | media | Renombrar la función interna a `Calcular_Metricas_Curva` (coincidiendo con el archivo) y actualizar el único call site (`Procesar_Multisujeto_Core.m:128`) a `Calcular_Metricas_Curva(...)`. | alta (archivo y call site leídos directamente) |
| `Test_Generador_Trayectoria.m:144` | ruta absoluta hardcodeada, no portable | `ruta_csv_real = 'C:\articuloq2\REFERENCIAS\Control_apoyo_Luis_V4.csv';` — falla en cualquier clon del repo que no esté exactamente en `C:\articuloq2` (p.ej. la Mac que `CLAUDE.md` documenta desde el 22-ago-2026, o cualquier otra ruta/unidad de Windows). Contrasta con el propio proyecto, que en otros archivos (`Yun2014_Wrapper.m:78-79`, `Cargar_Kuopio2024_Core.m`, etc.) sí resuelve rutas relativas a `mfilename('fullpath')`. | `Test_Generador_Trayectoria.m:144` | media | Reemplazar por una ruta derivada de `mfilename('fullpath')`, p.ej. `fullfile(fileparts(fileparts(mfilename('fullpath'))), '..', 'REFERENCIAS', 'Control_apoyo_Luis_V4.csv')` (mismo patrón ya usado en `Yun2014_Wrapper.m:78-79`). | alta (línea leída directamente; ya señalada también por A2 en `02_grafo.md` §4) |
| `CODIGOS/CALIBRACION/Test_Calibracion_Offset.m:18` | ruta absoluta hardcodeada, session-específica | `out_dir = 'C:\Users\chees\AppData\Local\Temp\claude\C--GAITSIM\92d083a2-3200-45db-906e-527d38dcbec4\scratchpad\datos_sinteticos_offset';` — apunta a un scratchpad de una sesión anterior de Claude Code, con el nombre de proyecto ANTIGUO (`GAITSIM`, antes de que el repo se renombrara a `articuloq2`, ver `CLAUDE.md` §"Estado actual", sesión 22-ago-2026). No rompe la ejecución (línea 19 hace `mkdir` si no existe), pero es una ruta ajena al repo, específica de una máquina/sesión de Claude Code que ya no existe, y con un nombre de proyecto obsoleto — confuso para cualquier otro colaborador o máquina, y el resultado del test queda fuera de cualquier carpeta versionada o predecible. | `CODIGOS/CALIBRACION/Test_Calibracion_Offset.m:18-19` | baja | Reemplazar por una carpeta relativa al repo (`fullfile(fileparts(mfilename('fullpath')), 'tmp_test_output')`) o `tempdir()` de MATLAB. | alta |
| `GRF_Newton_ApoyoSimple_Core.m` (archivo completo) y `MasaSegmentaria_DeLeva1996_Core.m` (archivo completo) | falta de prueba y de documentación, rompe convención propia del proyecto | Ambos son archivos `*_Core.m` nuevos (27-ago-2026, sin trackear en git). Ningún `Test_*.m` los ejercita (confirmado: `find CODIGOS -iname "Test_*.m"` no devuelve ningún test para "GRF" ni "Masa"), y A2 (`02_grafo.md` §1.6/§3) ya confirmó que ningún `.md` del proyecto (ni `GUIA_INTERPRETACION.md`, ni `CLAUDE.md`) los menciona todavía. Esto rompe la convención que el propio `CLAUDE.md` establece como regla ("ninguna carpeta de análisis se queda sin su propio `GUIA_INTERPRETACION.md`", patrón Core/Test/Guía usado en el resto de `GENERADOR/`, `CALIBRACION/`, `ESTADISTICA/`, etc.). Es particularmente notable en `GRF_Newton_ApoyoSimple_Core.m`, que hace diferenciación numérica de 2° orden con suavizado Savitzky-Golay y varias decisiones de implementación (framelen, extensión periódica con deriva) — exactamente el tipo de código que más se beneficia de una prueba con verdad conocida (mismo patrón que `Test_Calibracion_Offset.m` ya usa para `Calibracion_Offset_Core.m`). El propio archivo declara un autochequeo (`out.verificacion_media_vGRF_pctBW`, debería dar ~100%BW) pero no hay ningún script que lo verifique automáticamente. | `CODIGOS/GENERADOR/GRF_Newton_ApoyoSimple_Core.m:80-95` (campo de autochequeo declarado, sin test que lo llame); ausencia confirmada de `Test_GRF*.m`/`Test_Masa*.m` en el listado de archivos de `CODIGOS/`; `02_grafo.md` §1.6 (línea de hallazgo "ningún .md del propio proyecto... menciona estos 2 archivos nuevos") | media | Agregar `Test_GRF_Newton_ApoyoSimple.m` que (a) llame la función con antropometría sintética y verifique `verificacion_media_vGRF_pctBW ≈ 100` dentro de una tolerancia razonable, (b) verifique que `MasaSegmentaria_DeLeva1996_Core.m` da `verificacion_suma_pct ≈ 100` para M y F; documentar ambos en `GUIA_INTERPRETACION.md`. | alta (ausencia de test confirmada por listado de archivos, no inferida) |
| `Obtener_Angulos_Candidato.m:38-44` (comentario) referenciando `Evaluar_Mejor_Modelo_Rodilla.m` | posible desincronización silenciosa entre dos consumidores del mismo candidato | El comentario documenta que el 26-ago-2026 se corrigió esta función porque daba un ángulo tibial de Yun distinto al de `Evaluar_Mejor_Modelo_Rodilla.m` para el mismo candidato — es decir, ya hubo al menos una desincronización real entre los múltiples lugares que recalculan "qué vía usar por candidato" (`Obtener_Theta_Tibia_Candidato.m`, `Obtener_Angulos_Candidato.m`, y presumiblemente scripts de `RODILLA/`/`TOBILLO/`/`INCLINACION_TIBIAL/` que no se leyeron línea por línea en esta sesión, ver alcance). La causa raíz (que la regla E2 vive duplicada en más de un archivo en vez de un solo lugar) sigue presente tras la corrección puntual — el próximo cambio de regla puede volver a desincronizar alguno de los consumidores no actualizados. | `Obtener_Angulos_Candidato.m:38-44` | media | Consolidar la regla "qué vía usar por candidato y fase" en un único punto (p.ej. que `Obtener_Angulos_Candidato.m` llame internamente a `Obtener_Theta_Tibia_Candidato.m` para su theta_tibia en vez de reimplementar el switch-case de vías) — reduce a un lugar el riesgo de que un futuro cambio de regla deje otro consumidor desactualizado. | media (el propio comentario documenta el patrón de riesgo; no se verificó exhaustivamente si otros consumidores en `RODILLA/`/`TOBILLO/` ya están o no sincronizados, por alcance) |
| `Aplicar_SPM_BlandAltman_CurvasExistentes.m` (no invoca `BlandAltman_Core.m`) | informativo, ya documentado como esperado | Confirmado por A2 (`02_grafo.md` §1.2, fila "ROTO/NO-EJECUTADO (esperado, documentado)"): el script nunca llama a `BlandAltman_Core.m` pese a tener el nombre — consistente con `CLAUDE.md` ("con los datos que existen hoy... no se puede correr Bland-Altman"), no es un hallazgo nuevo. Se registra aquí solo para completitud de la tabla de código. | `CODIGOS/ESTADISTICA/Aplicar_SPM_BlandAltman_CurvasExistentes.m` (grep: 0 ocurrencias de `BlandAltman_Core`) | baja | Ninguna — comportamiento ya documentado y esperado por el propio proyecto; no ejecutar como si fuera Bland-Altman real hasta que haya datos pareados de dos instrumentos. | alta |

## CODIGOS/MULTISUJETO/, CODIGOS/ESTADISTICA/ (prioridad secundaria, cubiertos con lectura completa)

| archivo:línea | tipo | qué pasa | evidencia | severidad | corrección propuesta | confianza |
|---|---|---|---|---|---|---|
| `SPM1D_Core.m:311,323` | límite arbitrario documentado, no un bug | `T_MAX = 1000` acota `|t|` cuando la varianza entre ensayos es exactamente cero — ya documentado en el propio código y en `CLAUDE.md` (sesión 02-ago-2026) como corrección deliberada de un bug real anterior (`|t|~10^16`). Verificado que el comentario coincide con el comportamiento actual del código (no quedó un valor viejo tras la corrección). | `SPM1D_Core.m:300-318` | — (no es hallazgo, es verificación positiva) | ninguna | alta |
| `CODIGOS/MULTISUJETO/Cargar_Sujetos_CSV.m` (archivo completo, grado de entrada 0) | huérfano funcional esperado, no ejercitado por ningún test | Confirmado (A2 y esta sesión): es el único archivo `.m` de `MULTISUJETO/` que ningún otro `.m` invoca — ni siquiera `Test_Procesar_Multisujeto.m`, que usa datos sintéticos generados inline en vez de pasar por este cargador. `CLAUDE.md` ya lo documenta como "el único archivo que habrá que tocar cuando se defina el formato de carpetas real" — consistente con el estado real del código, no es una sorpresa, pero significa que esta función interactiva (parseo real de CSV, `uigetfile` implícito) NUNCA se ha ejecutado ni verificado automáticamente, ni con datos sintéticos ni reales. | `CODIGOS/MULTISUJETO/Cargar_Sujetos_CSV.m:1-40` (leído); ausencia de cualquier llamador confirmada en `02_grafo.md` §2-3 | media | Cuando se decida el formato de carpetas real (bloqueo ya documentado por el propio proyecto), agregar al menos una prueba con 1-2 archivos CSV sintéticos de ejemplo para confirmar que el parseo (columnas, atan2, Savitzky-Golay) funciona antes de depender de él con sujetos reales. | alta |

---

## Resumen

- **Archivos revisados en profundidad (lectura línea a línea completa o casi completa):** 24 — los 10 núcleo del generador señalados por `CLAUDE.md` más 14 archivos de soporte directo (`Reduccion_Winter_Core.m`, `Obtener_Theta_Tibia_Candidato.m`, `Obtener_Angulos_Candidato.m`, `Segmento_Posicion_Core.m`, `Temporizacion_Core.m`, `Tiempo_Ciclo_Koopman2014_Core.m`, `GRF_Newton_ApoyoSimple_Core.m`, `MasaSegmentaria_DeLeva1996_Core.m`, `Cargar_Camargo_Core.m`, `SPM1D_Core.m`, `Procesar_Multisujeto_Core.m`, `Cargar_Sujetos_CSV.m`, `Test_Calibracion_Offset.m`, `Calcular_Metricas_Curva.m`).
- **Archivos revisados superficialmente (grep dirigido, no lectura línea a línea):** ~68 — el resto de `CODIGOS/` (scripts `Evaluar_*`/`DIAG_*` de `RODILLA/`, `TOBILLO/`, `INCLINACION_TIBIAL/`, `Fukuchi/`, `Ferber/`, `Kuopio/`, y el resto de `CALIBRACION/`, `ESTADISTICA/`, `POTENCIA_EQUIVALENCIA/`, `INCERTIDUMBRE/`, `VALIDACIONES/`, `GENERAR CURVS DE REFERENCIA/`). No se ejecutó ningún script (regla 9); no se repitió el chequeo de sintaxis de `mlint` (ya cubierto por A1, 0 errores/35 avisos de estilo).
- **Hallazgos totales:** 10. Por severidad: **alta 2** (documentación desactualizada de la regla E2 en `Cadena_Cinematica_Core.m`/`GUIA_INTERPRETACION.md`; combinación de Romero-Sorozábal sin resolver la advertencia de signo de X propia del módulo), **media 6** (cómputo redundante del modelo de candidato, mismatch de mayúsculas `calcular_metricas_curva`, ruta absoluta no portable en `Test_Generador_Trayectoria.m`, falta de test/documentación para `GRF_Newton_ApoyoSimple_Core.m`/`MasaSegmentaria_DeLeva1996_Core.m`, riesgo de desincronización de la regla E2 entre múltiples consumidores, `Cargar_Sujetos_CSV.m` nunca ejercitado), **baja 2** (ruta de scratchpad de sesión antigua en `Test_Calibracion_Offset.m`; `Aplicar_SPM_BlandAltman_CurvasExistentes.m` sin invocar `BlandAltman_Core.m`, ya documentado como esperado).
- **Los 3 hallazgos más importantes:** (1) la documentación de la regla E2 "qué vía usar por candidato y fase" quedó desactualizada en dos lugares (`Cadena_Cinematica_Core.m` y `GUIA_INTERPRETACION.md` §3-bis) tras la corrección real del 24-ago-2026, con riesgo de que un futuro desarrollador (o el propio equipo) reimplemente la regla vieja confiando en esos comentarios; (2) `Combinar_Candidatos_Core.m` combina el eje X de Romero-Sorozábal 2024 sin resolver la advertencia de signo que el propio `Romero_Sorozabal2024_Core.m` declara explícitamente, y el plan de ensamble ya registra una discrepancia de hasta 60cm entre candidatos sin diagnosticar si esta es la causa — pone en duda cualquier resultado ya generado con `candidato='Combinado'`; (3) el pipeline recalcula el modelo completo del candidato dos veces por corrida (`Obtener_Theta_Tibia_Candidato` + `Obtener_Angulos_Candidato`), lo que para Yun implica correr el toolbox de terceros (GPR + I/O de 30 archivos) dos veces por cada llamada a `Generar_Trayectoria`/`GRF_Newton_ApoyoSimple_Core`.
- **No se encontraron:** errores de sintaxis (ya cubierto por A1/mlint), bucles infinitos, condiciones siempre verdaderas/falsas evidentes, ni `try/catch` que silencien errores sin registrarlos (grep de `catch` sin manejo: 0 coincidencias en `CODIGOS/`).
